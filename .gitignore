# Python bytecode. Running a validator or a test suite from inside the plugin
# tree creates these; they are build output, they are platform- and
# version-specific, and a plugin that ships them makes every installed copy
# differ from the repo for no reason.
__pycache__/
*.py[cod]
