# Course brief

*Type freely. Anything you leave out, the skill either derives or asks for — batched, not one
question at a time. Do not try to fill in the module split, screen counts, task codes or pass
marks: those are derived, and deriving them is the skill's job.*

---

## The one thing without which nothing can start

**Which approved programme governs this course?**
Name it, or give the path. The skill reads the academic-hour length, the topic/hours table and
the ILOs out of it. Without it there is no hours law and no ILOs.

> e.g. `knowledge_base/Programms and IMO Models/1. Program Basic Training Gas - Rev. 01/content.md`

---

## The sources

**Where is the knowledge base?** The docling-extracted folder for this subject.

**Any IMO model course that applies?** It is guidance — the approved programme still governs.
Name it and it will be cited as support.

---

## What is different from GAS BASIC

Write only what differs. Silence means "same as GAS BASIC".

- **Days and group size** —
- **Simulator?** which one, and what is it used for —
- **Practical facility?** live fire, enclosed-space rig, bench equipment —
- **Anything the school does not have** — say so; a gap gets logged, never invented

---

## Anything else

Constraints, a deadline, something that went wrong last time this course ran, a person whose
opinion decides an argument.

---

<!--
  What the skill does with this, in order (knowledge/build-order.json is the authority):
    programme -> hours -> ILO map -> module split -> GATE (you approve) -> screen inventory
    -> task pages -> screens -> handout -> assessment -> module plans -> terminals -> verify

  Three things it will hold you to, because each one cost real rework on GAS BASIC:
    1  every module's teaching minutes equal its accredited minutes EXACTLY; the overflow
       goes to the handout as self-study, never into the block and never silently cut
    2  Main ILOs and the study programme are verbatim; a re-expressed Sub-ILO is PROVISIONAL
       until you ratify it and is never renumbered where a trainee can see it
    3  80/20 practical is the target; where the programme makes it impossible, the theory
       hours are delivered as active learning - a theory block with no trainee activity in
       it is a defect
-->
